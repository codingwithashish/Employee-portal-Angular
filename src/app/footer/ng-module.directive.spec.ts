import { NgModuleDirective } from './ng-module.directive';

describe('NgModuleDirective', () => {
  it('should create an instance', () => {
    const directive = new NgModuleDirective();
    expect(directive).toBeTruthy();
  });
});
